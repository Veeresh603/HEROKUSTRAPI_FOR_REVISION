'use strict';

module.exports = {
    settings: {
        cors: {
            origin: ['http://localhost:8000', 'http://localhost:1337', 'https://certisured.com', 'https://dev-certisured.netlify.app'],
            credentials: true
        }
    }
}